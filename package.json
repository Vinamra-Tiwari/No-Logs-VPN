{
  "name": "nexus-vpn-contracts",
  "version": "1.0.0",
  "description": "Smart contracts and local dev chain for Nexus VPN audit ledger",
  "private": true,
  "scripts": {
    "node": "hardhat node",
    "compile": "hardhat compile",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.18"
  }
}
